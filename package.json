{
  "name": "remember",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "REMEMBER — Personal Knowledge Base built with VitePress.",
  "scripts": {
    "generate-toc": "node scripts/generate-toc.js",
    "generate-assets": "node scripts/generate-assets.js",
    "generate": "npm run generate-toc && npm run generate-assets",
    "dev": "npm run generate && vitepress dev docs",
    "build": "npm run generate && vitepress build docs",
    "serve": "vitepress serve docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
